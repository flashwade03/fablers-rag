---
name: ask
description: Ask a question about The Art of Game Design using the agentic RAG pipeline.
argument-hint: "<질문>"
---

# /ask Command

The user is asking: **$ARGUMENTS**

Execute the `ask` skill to answer this question using the full agentic RAG pipeline (query analysis → hybrid retrieval → reranking → CRAG validation → answer synthesis).
